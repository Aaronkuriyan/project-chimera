# Optional: enables LLM-powered bizarro-content generation via Groq's
# OpenAI-compatible API (fast + free tier, good for live demos).
# If unset, Chimera falls back to deterministic fabrication (no API needed).
GROQ_API_KEY=
